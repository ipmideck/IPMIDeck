// Stub created by Plan 02.1-02 so App.tsx's lazy import resolves and typechecks.
// Plan 02.1-03 overwrites this with the real full-screen /login page (D-04).
export default function LoginPage() {
  return null;
}
